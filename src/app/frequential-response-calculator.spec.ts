import { FrequentialResponseCalculator } from './frequential-response-calculator';

describe('FrequentialResponseCalculator', () => {
	it('should create an instance', () => {
		expect(new FrequentialResponseCalculator()).toBeTruthy();
	});
});
